<?php
ini_set('display_errors',1);
error_reporting(E_ALL);

$apiKey = 'bda7647c-53ce-4b2e-a0b7-3d24f2d60fee';
$country = 'Indonesia';
//$state = 'Jakarta'; $city = 'Jakarta';
$state = 'Bali'; $city = 'Denpasar';

$url = "https://api.airvisual.com/v2/city?city={$city}&state=Jakarta&country={$country}&key={$apiKey}";

$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, $url);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
$response = curl_exec($ch);
$httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
curl_close($ch);

echo $httpCode."<br>";
echo $url."<br>";

if ($httpCode === 200) {
    $data = json_decode($response, true);
	$status = ($data['status'] && $data['status'] == "success") ?? 1 : 0;
	
	var_dump($status);
	echo "<pre>";
	print_r($data);
	echo "</pre>";
	
    if ($status) {
        $res_country = $data['data']['country'];
        $res_state = $data['data']['state'];
		$res_city = $data['data']['city'];
        $res_current = $data['data']['current'];
		
		$weather = $res_current['data']['weather'];

        echo "📍 Lokasi: " . $res_city . "\n";
        echo "Waktu: " . $weather['ts'] . "\n";
        echo "Suhu: " . $weather['tp'] . "°C\n";
        echo "Kelembapan: " . $weather['hu'] . "%\n";
        echo "Tekanan Udara: " . $weather['pr'] . " hPa\n";
        //echo "Awan: " . $weather['cloud_cover']['value'] . "%\n";
        echo "Kecepatan Angin: " . $weather['ws'] . " km/h\n";
        echo "Arah Angin: " . $weather['wd'] . "°\n";
    } else {
        echo "❌ Data tidak ditemukan untuk kota {$city}\n";
    }
} else {
    echo "❌ HTTP Error Code: {$httpCode}\n";
    echo "Response: {$response}\n";
}