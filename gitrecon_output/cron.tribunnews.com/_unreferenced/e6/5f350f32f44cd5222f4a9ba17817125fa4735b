<?php
include_once "/var/www/html/web-cron/config/config.php";

//RDS
class config_db_tjb{
    private $db_host_dev;
    private $db_username_dev;
    private $db_password_dev;
    private $db_host_prod;
    private $db_username_prod;
    private $db_password_prod;

    private $db_blog_host_dev;
    private $db_blog_username_dev;
    private $db_blog_password_dev;
    private $db_blog_host_prod;
    private $db_blog_username_prod;
    private $db_blog_password_prod;
    

    public function __construct()
    {
        $this->db_host_dev = RDS_TJB_HOST_DEV;
        $this->db_username_dev = RDS_TJB_USERNAME_DEV;
        $this->db_password_dev = RDS_TJB_PASSWORD_DEV;

        $this->db_host_prod = RDS_TJB_HOST_MASTER_PROD;
        $this->db_username_prod = RDS_TJB_USERNAME_MASTER_PROD;
        $this->db_password_prod = RDS_TJB_PASSWORD_MASTER_PROD;

        $this->db_blog_host_dev = RDS_TJB_BLOG_HOST_MASTER_DEV;
        $this->db_blog_username_dev = RDS_TJB_BLOG_USERNAME_MASTER_DEV;
        $this->db_blog_password_dev = RDS_TJB_BLOG_PASSWORD_MASTER_DEV;

        $this->db_blog_host_prod = RDS_TJB_BLOG_HOST_MASTER_PROD;
        $this->db_blog_username_prod = RDS_TJB_BLOG_USERNAME_MASTER_PROD;
        $this->db_blog_password_prod = RDS_TJB_BLOG_PASSWORD_MASTER_PROD;
    }
    
    public function conn_to_db_tjb_dev()
    {
        $db_name_dev = "jualbeli-stag";
        $mysqli = new mysqli($this->db_host_dev, $this->db_username_dev,$this->db_password_dev,$db_name_dev);
        if ($mysqli->connect_errno) {
            echo "Failed to connect to MySQL: " . $mysqli -> connect_error;
            exit();
        }

        return $mysqli;
    }

    public function conn_to_db_tjb_prod()
    {
        $db_name_prod = "jualbeli";
        $mysqli = new mysqli($this->db_host_prod, $this->db_username_prod,$this->db_password_prod,$db_name_prod);
        if ($mysqli->connect_errno) {
            echo "Failed to connect to MySQL: " . $mysqli -> connect_error;
            exit();
        }

        return $mysqli;
    }

    public function conn_to_db_blog_tjb_dev()
    {
        $db_name_dev = "jualbeli-blog";
        $mysqli = new mysqli($this->db_blog_host_dev, $this->db_blog_username_dev,$this->db_blog_password_dev,$db_name_dev);
        if ($mysqli->connect_errno) {
            echo "Failed to connect to MySQL: " . $mysqli -> connect_error;
            exit();
        }

        return $mysqli;
    }

    public function conn_to_db_blog_tjb_prod()
    {
        $db_name_prod = "jualbeli-blog";
        $mysqli = new mysqli($this->db_blog_host_prod, $this->db_blog_username_prod,$this->db_blog_password_prod,$db_name_prod);
        if ($mysqli->connect_errno) {
            echo "Failed to connect to MySQL: " . $mysqli -> connect_error;
            exit();
        }

        return $mysqli;
    }


}


?>